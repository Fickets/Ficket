apiVersion: v1
kind: Service
metadata:
  name: spring-config-service
spec:
  selector:
    app: spring-config
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
  type: LoadBalancer
